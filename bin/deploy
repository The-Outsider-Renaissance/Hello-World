#!/usr/bin/env bash

set -e

id="${id:-}"

if [[ "$id" == "boom" ]]
then
    sleep 3
    echo "$id failed"
    exit 1
fi

echo "Simulating a Deployment $id - pretending to do something for a few seconds"
for i in $(seq 1 10)
do
    echo "$id $i"
    sleep 1
done

echo "done."
